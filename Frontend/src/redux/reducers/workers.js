//import { LOAD_WORKERS } from '../actionTypes';

const initialState = {
    workers: [],
};

export default function(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
