//import { LOAD_WORKS } from '../actionTypes';

const initialState = {
    works: [],
};

export default function(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
